/*
• Have a project's array to manage all of the projects
  - This will allow them to interact with the DOM
• Place todo object in project's array?
*/

export const projManager = function (project) {
  const projArr = [];
  projArr.push(project);

  console.log(projArr);
};
